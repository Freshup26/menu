rules_version = '2';

// ⚠️ هذه القواعد مفتوحة للجميع (أنت اخترت "مفتوحة عادي")
// أي شخص يقدر يعدّل المنيو من المتصفح. للأمان لاحقاً راجع README.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
